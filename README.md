###  属性

```xml
<attr name="radius" format="dimension" />
<attr name="background"/>
<attr name="background_color" format="color"/>
<attr name="stroke_width" format="dimension" />
<attr name="stroke_color" format="reference|color" />
<attr name="ripple_color" format="reference|color" />
<attr name="radius_l_u" format="dimension" />
<attr name="radius_l_d" format="dimension" />
<attr name="radius_r_u" format="dimension" />
<attr name="radius_r_d" format="dimension" />
<attr name="wv_start_color" format="reference|color" />
<attr name="wv_end_color" format="reference|color" />
<attr name="wv_color_orientation" format="enum">
   <!-- draw the gradient from the top to the bottom -->
   <enum name="TOP_BOTTOM" value="1" />
   <!-- draw the gradient from the top-right to the bottom-left -->
   <enum name="TR_BL" value="2" />
   <!-- draw the gradient from the right to the left -->
   <enum name="RIGHT_LEFT" value="3" />
   <!-- draw the gradient from the bottom-right to the top-left -->
   <enum name="BR_TL" value="4" />
   <!-- draw the gradient from the bottom to the top -->
   <enum name="BOTTOM_TOP" value="5" />
   <!-- draw the gradient from the bottom-left to the top-right -->
   <enum name="BL_TR" value="6" />
   <!-- draw the gradient from the left to the right -->
   <enum name="LEFT_RIGHT" value="7" />
   <!-- draw the gradient from the top-left to the bottom-right -->
   <enum name="TL_BR" value="8" />
</attr>


```
